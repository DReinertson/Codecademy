const plantNeedsWater = day => day === 'Wednesday' ? true : false;

//If single-line block code can remove return keyword and the curly "{}" brackets.
//If only single variable, no need for parentheses. Only need if no variable or multiple.
